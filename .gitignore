# dependencies
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env.local
.env.*.local

# prisma generated client
packages/db/src/generated/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# test / coverage
coverage/

# one-time API-Football importer resume checkpoints (never commit)
.import-progress.json
.tournament-progress.json
.diff-import-progress.json
.enrich-progress.json
.run-logs/

# docker volume (if mapped locally)
.pgdata/

# Quick Play bot avatar SOURCE images (large originals, ~85MB). The seed
# (apps/game-server/src/bots/seed-bots.ts) downscales these into the DB, so the
# originals only need to exist on the machine that runs the seed — never in git
# or on the server. identities.json IS committed; the images are not.
apps/game-server/src/bots/avatars/

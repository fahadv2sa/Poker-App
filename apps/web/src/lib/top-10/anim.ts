/**
 * Master switch for the Top Ten table's live-play flourishes (fly showers, confetti).
 * Set to false to disable them all in one move; the table still plays identically, just
 * without the celebration FX. Reduced-motion is handled separately by framer's
 * MotionConfig + the confetti CSS guard.
 */
export const TT_ANIMATIONS_ENABLED = true;
